# Node 版本管理工具切换功能使用指南

## 功能概述

这个功能允许你在不同的 Node.js 版本管理工具之间自由切换，并自动迁移你的 Node 版本和全局包。

支持的工具：
- **NVM for Windows** - Node Version Manager for Windows
- **fnm** - Fast Node Manager（推荐，速度最快）
- **Volta** - JavaScript 工具管理器

## 使用方法

### 方式 1：通过顶部工具栏

1. 打开 Node 管理页面
2. 点击右上角的 **切换图标** (⇄)
3. 在对话框中选择目标工具
4. 点击"开始迁移"

### 方式 2：通过版本卡片

1. 在"当前活动版本"卡片中
2. 点击版本管理工具名称旁边的 **"切换"** 按钮
3. 选择目标工具并开始迁移

## 迁移流程

自动化迁移包含以下步骤：

1. **检测当前环境**
   - 识别当前使用的版本管理工具
   - 获取当前 Node 版本
   - 列出所有已安装的版本

2. **保存状态**
   - 记录当前 Node 版本
   - 记录所有已安装版本
   - 备份全局包列表

3. **卸载旧工具**
   - 停止相关进程
   - 删除安装目录
   - 清理环境变量（部分需要手动）

4. **安装新工具**
   - 通过 winget 自动安装（fnm/volta）
   - NVM 需要手动安装

5. **恢复版本**
   - 自动安装之前的所有 Node 版本
   - 切换到之前使用的版本

6. **完成**
   - 显示迁移日志
   - 提示重启终端

## 注意事项

### ⚠️ 重要提示

1. **需要管理员权限**
   - 建议以管理员身份运行应用

2. **需要 winget**
   - fnm 和 volta 通过 winget 自动安装
   - 确保 Windows 10/11 已安装 winget

3. **NVM 需要手动安装**
   - NVM for Windows 无法通过 winget 安装
   - 需要从官网下载：https://github.com/coreybutler/nvm-windows/releases

4. **必须重启终端**
   - 环境变量更改需要重启终端才能生效
   - 建议重启所有打开的终端窗口

5. **备份重要数据**
   - 虽然会自动备份，但建议手动记录重要配置
   - 全局包会自动记录但不会自动重装

## 推荐工具

### fnm (Fast Node Manager) ⭐ 推荐

**优点：**
- 速度最快（Rust 编写）
- Windows 支持最好
- 自动切换版本（基于 .nvmrc）
- 安装简单

**适合：** 大多数开发者

### Volta

**优点：**
- 自动管理项目工具链
- 团队协作友好
- 跨平台一致性好

**适合：** 团队项目、多工具管理

### NVM for Windows

**优点：**
- 最流行的工具
- 社区支持好
- 功能完整

**缺点：**
- 速度较慢
- 需要手动安装

## 常见问题

### Q: 切换后 node 命令找不到？
A: 请重启终端，环境变量需要重新加载。

### Q: 全局包丢失了？
A: 全局包已备份但不会自动重装，可以查看日志中的包列表手动安装。

### Q: 可以回退到之前的工具吗？
A: 可以，再次运行切换功能即可。

### Q: 切换失败怎么办？
A: 查看日志输出，根据错误信息手动处理。迁移状态已保存，可以继续完成。

## 手动清理（如果需要）

如果自动卸载失败，可以手动清理：

### NVM for Windows
```powershell
# 删除目录
Remove-Item -Recurse -Force $env:NVM_HOME
Remove-Item -Recurse -Force $env:NVM_SYMLINK

# 清理环境变量（系统设置 > 环境变量）
# 删除: NVM_HOME, NVM_SYMLINK
# 从 PATH 中删除 NVM 相关路径
```

### fnm
```powershell
# 卸载
winget uninstall Schniz.fnm

# 删除数据目录
Remove-Item -Recurse -Force $env:USERPROFILE\.fnm
```

### Volta
```powershell
# 卸载
winget uninstall Volta.Volta

# 删除数据目录
Remove-Item -Recurse -Force $env:USERPROFILE\.volta
```

## 技术细节

### 迁移状态保存位置
```
%USERPROFILE%\Documents\node_migration_state.json
```

### 支持的命令

**检测工具：**
- `where nvm` / `where fnm` / `where volta`

**版本管理：**
- `nvm list` / `fnm list` / `volta list node`
- `nvm install` / `fnm install` / `volta install`
- `nvm use` / `fnm use` / `volta pin`

**安装/卸载：**
- `winget install Schniz.fnm`
- `winget install Volta.Volta`
- `winget uninstall <package>`

## 反馈与支持

如果遇到问题或有改进建议，请：
1. 查看日志输出
2. 检查环境变量配置
3. 提交 Issue 到项目仓库

---

**祝你切换顺利！** 🚀
