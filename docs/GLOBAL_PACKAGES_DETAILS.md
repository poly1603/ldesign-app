# 全局包详细信息功能

## 功能概述

为全局包列表添加详细信息查看功能，点击任意全局包可以查看：
- 📝 包描述
- 👤 作者信息
- 📜 许可证
- 🏠 主页链接
- 🔖 关键词标签
- 📦 NPM 页面链接

## 使用方法

### 方法 1: 点击包名

直接点击全局包列表中的任意包名，会弹出详细信息对话框。

### 方法 2: 点击信息图标

点击每个包右侧的 ℹ️ 图标，查看详细信息。

## 界面设计

### 详细信息对话框

```
┌─────────────────────────────────────────────┐
│ 📦 pnpm                              ✕     │
│    v10.26.0                                 │
├─────────────────────────────────────────────┤
│                                             │
│ 📝 描述                                     │
│ ┌─────────────────────────────────────────┐ │
│ │ Fast, disk space efficient package      │ │
│ │ manager                                 │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 👤 作者                                     │
│ ┌─────────────────────────────────────────┐ │
│ │ Zoltan Kochan                           │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 📜 许可证                                   │
│ ┌─────────────────────────────────────────┐ │
│ │ MIT                                     │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 🏠 主页                                     │
│ ┌─────────────────────────────────────────┐ │
│ │ https://pnpm.io              📋         │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 关键词                                      │
│ [package manager] [npm] [pnpm] [yarn]      │
│                                             │
│ 🔗 NPM 页面                                 │
│ ┌─────────────────────────────────────────┐ │
│ │ https://npmjs.com/package/pnpm  📋     │ │
│ └─────────────────────────────────────────┘ │
│                                             │
├─────────────────────────────────────────────┤
│                    [复制包名]  [关闭]       │
└─────────────────────────────────────────────┘
```

## 功能特性

### 1. 实时获取信息

使用 `npm view` 命令从 NPM registry 获取最新的包信息：

```bash
npm view <package-name> name version description author homepage license keywords --json
```

### 2. 加载状态

获取信息时显示加载动画：

```
┌─────────────────────┐
│  ⏳                 │
│  正在获取包信息...   │
└─────────────────────┘
```

### 3. 信息展示

#### 描述（Description）
- 📝 图标
- 灰色背景卡片
- 可选择文本

#### 作者（Author）
- 👤 图标
- 支持字符串和对象格式
- 自动提取作者名称

#### 许可证（License）
- 📜 图标
- 显示开源许可证类型
- 常见：MIT, Apache-2.0, ISC 等

#### 主页（Homepage）
- 🏠 图标
- 蓝色链接样式
- 点击复制到剪贴板
- 显示复制图标

#### 关键词（Keywords）
- 🔖 标签样式
- 彩色背景
- 圆角设计
- 自动换行

#### NPM 页面
- 🔗 图标
- 自动生成链接
- 点击复制

### 4. 交互功能

#### 复制链接
点击任意链接区域，自动复制到剪贴板：
```
✅ 链接已复制到剪贴板
```

#### 复制包名
底部操作栏提供"复制包名"按钮：
```
✅ 已复制: pnpm
```

### 5. 响应式设计

- 对话框宽度：600px
- 最大高度：700px
- 内容区域可滚动
- 适配不同屏幕尺寸

## 数据处理

### Author 字段处理

NPM 的 author 字段可能有多种格式：

```json
// 格式 1: 字符串
"author": "Zoltan Kochan"

// 格式 2: 对象
"author": {
  "name": "Zoltan Kochan",
  "email": "z@kochan.io",
  "url": "https://kochan.io"
}
```

代码自动处理两种格式：

```dart
String? authorName;
if (author is String) {
  authorName = author;
} else if (author is Map) {
  authorName = author['name'] as String?;
}
```

### Keywords 处理

关键词以标签形式展示：

```dart
Wrap(
  spacing: 8,
  runSpacing: 8,
  children: keywords.map((keyword) {
    return Container(
      padding: EdgeInsets.symmetric(horizontal: 12, vertical: 6),
      decoration: BoxDecoration(
        color: colorScheme.secondaryContainer,
        borderRadius: BorderRadius.circular(16),
      ),
      child: Text(keyword.toString()),
    );
  }).toList(),
)
```

## UI 组件

### 信息区块（Info Section）

```dart
_buildInfoSection(
  context,
  '描述',                    // 标题
  Icons.description_outlined, // 图标
  description,                // 内容
  colorScheme,
)
```

特点：
- 图标 + 标题
- 灰色背景卡片
- 可选择文本
- 圆角设计

### 链接区块（Link Section）

```dart
_buildLinkSection(
  context,
  '主页',                  // 标题
  Icons.home_outlined,     // 图标
  homepage,                // URL
  colorScheme,
)
```

特点：
- 蓝色主题
- 下划线样式
- 点击复制
- 复制图标提示

## 错误处理

### 获取失败

如果 `npm view` 命令失败：

```
❌ 获取 <package-name> 信息失败
```

### 网络问题

如果网络连接失败：

```
❌ 获取包信息失败: <error>
```

## 性能优化

### 1. 按需加载

只在用户点击时才获取详细信息，不预加载。

### 2. 加载提示

显示加载动画，提升用户体验。

### 3. 错误恢复

获取失败时优雅降级，不影响其他功能。

## 使用场景

### 场景 1: 了解包的用途

```
用户：这个 @ldesign/doc 是什么？
操作：点击包名
结果：看到描述"Documentation generator for ldesign"
```

### 场景 2: 查找包的主页

```
用户：想看 pnpm 的官方文档
操作：点击包名 → 查看主页链接
结果：复制 https://pnpm.io 到浏览器
```

### 场景 3: 检查许可证

```
用户：这个包是什么许可证？
操作：点击包名 → 查看许可证
结果：看到 "MIT"
```

### 场景 4: 查找相关包

```
用户：想找类似的包
操作：点击包名 → 查看关键词
结果：看到 [package manager] [npm] [pnpm]
      可以搜索这些关键词找到相关包
```

## 对比

### 之前 ❌

```
全局包列表：
- pnpm v10.26.0
- yarn v1.22.22
- typescript v5.3.3

只能看到名称和版本，不知道包的用途。
```

### 现在 ✅

```
全局包列表：
- pnpm v10.26.0  [ℹ️]
  ↓ 点击
  
详细信息：
✅ 描述：Fast, disk space efficient package manager
✅ 作者：Zoltan Kochan
✅ 许可证：MIT
✅ 主页：https://pnpm.io
✅ 关键词：package manager, npm, pnpm
✅ NPM 页面：https://npmjs.com/package/pnpm
```

## 技术实现

### 1. 获取包信息

```dart
final result = await Process.run(
  'npm',
  ['view', packageName, 'name', 'version', 'description', 
   'author', 'homepage', 'license', 'keywords', '--json'],
  runInShell: true,
);
```

### 2. 解析 JSON

```dart
final json = jsonDecode(result.stdout.toString()) as Map<String, dynamic>;

final version = json['version'] as String?;
final description = json['description'] as String?;
final author = json['author'];
final homepage = json['homepage'] as String?;
final license = json['license'] as String?;
final keywords = json['keywords'] as List<dynamic>?;
```

### 3. 显示对话框

```dart
showDialog(
  context: context,
  builder: (context) => Dialog(
    child: Container(
      width: 600,
      constraints: BoxConstraints(maxHeight: 700),
      child: Column(
        children: [
          // 标题栏
          // 内容区域
          // 操作栏
        ],
      ),
    ),
  ),
);
```

## 修改的文件

1. `lib/presentation/pages/node/node_page.dart`
   - 添加 `_showGlobalPackageDetails` 方法
   - 添加 `_showPackageDetailsDialog` 方法
   - 添加 `_buildInfoSection` 方法
   - 添加 `_buildLinkSection` 方法
   - 修改全局包列表项，添加点击事件
   - 添加信息图标按钮

## 总结

### 新增功能

✅ 点击查看包详情  
✅ 显示包描述  
✅ 显示作者信息  
✅ 显示许可证  
✅ 显示主页链接  
✅ 显示关键词标签  
✅ 显示 NPM 页面链接  
✅ 一键复制链接  
✅ 一键复制包名  

### 用户价值

📚 **了解包的用途** - 不用去 NPM 网站查  
👤 **知道包的作者** - 了解包的维护者  
📜 **检查许可证** - 确保合规使用  
🔗 **快速访问文档** - 复制主页链接  
🔍 **发现相关包** - 通过关键词搜索  

---

**现在全局包列表不只是一个简单的列表，而是一个信息丰富的包管理中心！** 📦✨
